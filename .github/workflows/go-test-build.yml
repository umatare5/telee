name: Test and Build
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    uses: umatare5/common/.github/workflows/go-test-build.yml@7cfc52a86a80be5604880082503cf36fada25704 # v0.14.0
    with:
      go_version: "1.25.2"
