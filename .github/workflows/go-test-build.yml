name: Test and Build
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    uses: umatare5/common/.github/workflows/go-test-build.yml@ec6477148fb47ffc8b34a89dfdc2ec725a82863a # v0.18.0
    with:
      go_version: "1.25.2"
